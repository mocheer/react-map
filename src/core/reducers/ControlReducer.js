const ControlOptions = {
    zoomControl:true,
    scaleControl:true,
    navigatorControl:false
}
/**
 * 
 */
export function ControlReducer(state=ControlOptions, action){
    switch(action.type){
        default:
           return state;
    }
};